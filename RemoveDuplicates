/*
TC; O(n)
SC: (1)
Thought process: this question was similar to move zeros, so used fast and slow pointers.
Generally this algorithm will have one slow pointer which checks the validity of condition and replaces
element in fast pointer.
 */

class RemoveDuplicates{
    public int removeDuplicates(int[] nums) {
        int count = 1, j = 1;

        for (int i = 1; i < nums.length; i++){
            if( nums[i-1] == nums[i]){
                count++;
            }

            else{
                count = 1;
            }
            if (count <= 2){
                nums[j] = nums[i];
                j++;
            }

        }
        return j;
    }

}